import {useEffect,useState} from 'react';
import {supabase} from '../lib/supabase';
export default function ProductionCheck(){const[status,setStatus]=useState('Checking…'),[result,setResult]=useState(null);useEffect(()=>{(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){setStatus('Please sign in as an admin.');return}const{data,error}=await supabase.rpc('run_production_readiness_check');if(error){setStatus(error.message);return}setResult(data);setStatus(data.status==='ready_for_application_testing'?'Security checks passed.':'Security fixes still required.');})()},[]);return <main className="content"><span className="eyebrow">PRODUCTION READINESS</span><h1>Security Check</h1><p className="lead">This check must be run while authenticated as an admin.</p><div className="card"><h2>{status}</h2>{result&&<pre>{JSON.stringify(result,null,2)}</pre>}</div></main>}
